package com.lembed.unit.test.core;

public class PLANS {

	
	
	/**
	 * The Unit test project build from template project, the compiler options, target setting, etc
	 * inherit from the template project.
	 * 
	 * 
	 * @@ compiler symbols
	 *  1, The easy way is to get the symbols from target project in local workspace
	 *  
	 *  2, Get the the target path, and copy the setting from target 
	 * 
	 * @@ 
	 */
	
	
}
